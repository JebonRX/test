Setup:
```
apt install -y screen wget curl && wget https://raw.githubusercontent.com/JebonRX/test/main/install/setup.sh && chmod +x setup.sh && sed -i -e 's/\r$//' setup.sh && screen -S setup ./setup.sh
```

⏩ XRAY MULTIPORT AUTOSCRIPT ⏪

[ VPS SERVICES ]

♦️ XRAY VMESS WEBSOCKET TLS / NTLS 443/80

♦️ XRAY VLESS WEBSOCKET TLS / NTLS 443/80

♦️ XRAY VLESS HTTPUGRADE TLS / NTLS 443/80

♦️ SSH WEBSOCKET TLS / NTLS 443/80
